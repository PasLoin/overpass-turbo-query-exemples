[timeout:280][bbox:{{bbox}}];
way[highway=construction][opening_date]
    (if:is_date(t["opening_date"]) && 
        date(t["opening_date"])<date("{{date:0 days}}"));
(._;>;); out meta;/*fixed by auto repair*/
